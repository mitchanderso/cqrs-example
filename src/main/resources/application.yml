server:
  servlet:
    context-path: /api/v1

spring:
  h2:
    console:
      enabled: true
      path: /console/

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
#
#
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
